<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Student_Manager.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Student_Manager.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Image Source="../Assets/csu-icon.gif"/>
            <TextBlock Margin="10,0,0,0" VerticalAlignment="Center" Text="Student Manager"/>
        </StackPanel>
        <ScrollView Grid.Row="1">
            <StackPanel VerticalAlignment="Center">
                <Image Source="../Assets/CSU.gif" Height="200" HorizontalAlignment="Center"/>
                <TextBlock Text="Welcome!" FontSize="32" HorizontalAlignment="Center"/>
                <TextBox x:Name="nameTextbox" Width="200" PlaceholderText="Write your full name" Margin="10"/>
                <Button x:Name="loginButton" Content="Pasok po" HorizontalAlignment="Center" Click="LoginButton_Click"/>
            </StackPanel>
        </ScrollView>
    </Grid>
</Page>
